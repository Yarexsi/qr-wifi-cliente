# qr-only

Proyecto mínimo que contiene solo el Generador de QR WiFi, independiente del sistema principal.

Instalación y ejecución:

```bash
cd "c:\Users\yarex\Documents\RED7P\qr-only"
npm install
npm run dev
```

El servidor corre en el puerto `3001` por defecto.
